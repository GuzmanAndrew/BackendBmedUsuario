package com.medkaapp.security.repository;

import com.medkaapp.security.entity.TipoPatologia;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoPatologiaRepository extends JpaRepository<TipoPatologia, Long> {
}
